# LOCAL -------------------------------------
# Django Settings
# SECRET_KEY=django-insecure-development-key
# DEBUG=True
# ALLOWED_HOSTS=localhost,127.0.0.1

# # Database Configuration
# # --- SQLite for default dev
# DB_ENGINE=django.db.backends.sqlite3
# DB_NAME=db.sqlite3
# DB_USER=postgres
# DB_PASSWORD=postgres
# DB_HOST=localhost
# DB_PORT=5432

# # --- PostgreSQL (uncomment and fill in if using docker-compose.dev.yml or docker-compose.yml)
# # DB_ENGINE=django.db.backends.postgresql
# # DB_NAME=esg_scoring
# # DB_USER=postgres
# # DB_PASSWORD=postgres
# # DB_HOST=localhost        # use 'localhost' for docker-compose.dev.yml
# # DB_HOST=db               # use 'db' for docker-compose.yml
# # DB_PORT=5432

# # CORS Settings
# # Comma-separated list of origins allowed to make cross-origin requests
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# # Redis Cache Settings
# # For Docker, use: redis://redis:6379/1
# REDIS_URL=redis://127.0.0.1:6379/1
# ------------------------------------------


# PRODUCTION -------------------------
DEBUG=False
PRODUCTION_MODE=True
SECRET_KEY=generate-secret-difficult-key
ALLOWED_HOSTS=esg.kbtu.kz,web

DB_ENGINE=django.db.backends.postgresql
DB_NAME=esg_scoring
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432

REDIS_URL=redis://redis:6379/1